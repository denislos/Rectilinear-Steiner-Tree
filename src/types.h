

#ifndef TYPES_H
#define TYPES_H

#include <cstdint>
#include <limits>

using coord_type = int;

constexpr static const std::size_t DIST_INF = std::numeric_limits<std::size_t>::max();

#endif // TYPES_H